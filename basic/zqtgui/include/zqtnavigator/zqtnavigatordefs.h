#ifndef ZQTNAVIGATORDEFS_H
#define ZQTNAVIGATORDEFS_H

#include <QIcon>

// 定义自定义用户角色，用于在模型中存储特定数据
const int IconRole = Qt::UserRole + 1;

// 定义按钮的几何信息
const int BUTTON_WIDTH = 20;
const int BUTTON_HEIGHT = 20;
const int BUTTON_MARGIN = 5; // 按钮之间的间距

#endif // ZQTNAVIGATORDEFS_H